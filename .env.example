# Created automatically by `npx convex dev --configure new`.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
# Google OAuth Web application client ID (public, not a client secret).
VITE_GOOGLE_CLIENT_ID=
# Keep false for a shared production game. Local Vite dev always offers preview.
VITE_ENABLE_PREVIEW=false
